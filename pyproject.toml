[project]
name = "waveguide-opt"
version = "0.1.0"
description = "ATH/ABEC automation helpers for T34B + PTT8 waveguide optimization"
requires-python = ">=3.11"

[project.scripts]
waveguide-opt = "waveguide_opt.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"
